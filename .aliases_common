alias gb='git branch'
alias gst='git status'
alias gfe='git fetch -p'
alias gco='git checkout'
alias gdi='git diff'
alias gpul='git pull'
alias gpush='git push'
alias glazypush='git pull; git ls-files -co --exclude-standard | grep '\.md$' | xargs git add; git commit --allow-empty-message -m ""; git push'

alias v='vim'
alias c='code'

alias ll='ls -al'
alias mkdir='mkdir -p'
alias cd.='cd ..'
alias pg='pgrep -fl'
alias pk='pkill -fl'
alias resh='exec -l $SHELL'

alias ghub='hub browse'
alias gh='ghq list | peco | cut -d "/" -f 2,3 | xargs hub browse'

alias til='c ~/Git/github.com/teriyakiegg/til-v2'
alias dot='c ~/Git/github.com/teriyakiegg/dotfiles'

alias al='v ~/.aliases_common'
alias lo='v ~/.aliases_local'

alias fig='docker-compose'

alias gr='go run *.go'

alias r='rails'
alias be='bundle exec'
